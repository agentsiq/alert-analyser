apiVersion: v1
kind: Service
metadata:
  name: alert-analyser
  namespace: ${NAMESPACE:-ai-agents}
  labels:
    app: alert-analyser
spec:
  type: ClusterIP
  selector:
    app: alert-analyser
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
